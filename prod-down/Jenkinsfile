pipeline {
    agent any

    environment {
        SWARM_MANAGER = '172.24.140.86:2375'
        STACK_NAME = 'sbms-prod'
    }
    
    stages {
        stage('Tear Down Prod') {
            steps {
                sh "docker -H $SWARM_MANAGER stack rm $STACK_NAME"
            }
        }
    }
}
