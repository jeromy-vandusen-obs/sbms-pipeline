pipeline {
    agent any

    environment {
        SWARM_MANAGER = '172.24.140.88:2375'
        APP_STACK_NAME = 'sbms-uat-app'
        INFRA_STACK_NAME = 'sbms-uat-infra'
    }
    
    stages {
        stage('Tear Down UAT') {
            steps {
                sh "docker -H $SWARM_MANAGER stack rm $APP_STACK_NAME"
                sh "docker -H $SWARM_MANAGER stack rm $INFRA_STACK_NAME"
            }
        }
    }
}
